export function resetDashboardScroll() { if (typeof window === 'undefined') return; const behavior = window.matchMedia?.('(prefers-reduced-motion: reduce)').matches ? 'auto' : 'smooth'; window.requestAnimationFrame(() => window.scrollTo({ top: 0, left: 0, behavior })) }
